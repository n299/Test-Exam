<% flash.each do |key, value| %>
  <% if key.present? %>
    <div class="container text-center">
      <p class="alert flash-message alert-<%= check_flash(key) %> mt-3">
        <%= value %>
        <button class="close" data-dismiss="alert">×</button>
      </p>
    </div>
  <% end %>
<% end %>
